---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (unknown)
  name: vmvolumes.kcrow.io
spec:
  group: kcrow.io
  names:
    kind: VmVolume
    listKind: VmVolumeList
    plural: vmvolumes
    singular: vmvolume
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VmVolume is the Schema for the VmVolumes API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: translate mount to device and hooks VmVolumeSpec defines
              the desired state of resource.
            properties:
              runtimeClassName:
                items:
                  type: string
                type: array
              volumeMap:
                additionalProperties:
                  type: string
                description: storageclass name and voltype voltype will be used in
                  hook command, like mount
                type: object
            type: object
          status:
            description: VmVolumeStatus defines the observed state of resource.
            properties:
              volume:
                items:
                  properties:
                    persistentVolume:
                      type: string
                    pod:
                      type: string
                    type:
                      type: string
                    volume:
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
